function playlistTracksContainer() {
    const tracksContainerElement = document.createElement('div')

    tracksContainerElement.className = 'playlist-tracks'

    divContainerElements.append(tracksContainerElement)
}